{
  "name": "boilerplate-node",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start:api": "lerna exec --scope @boilerplate-node/api yarn start",
    "start:worker": "lerna exec --scope @boilerplate-node/worker yarn start"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.0.0"
  }
}
